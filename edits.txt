create table ==> names shouldn't take special char except _ (checked)
select table ==> "Example: SELECT col1,col2 FROM $table WHERE col=value"
			   ----------------------------
			   		remake
insert table ==> modify check for pk compositions ==> make separate script to validate and reuse

update, delete ==> to be made
